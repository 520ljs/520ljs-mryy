# 端口号
server:
  port: 8080
spring:
  datasource:
    # 加载数据库驱动
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 地址-数据库名-时差-编码格式
    url: jdbc:mysql://localhost:3306/qf-ljs-mr?serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: root

# Mybatis扫描mapper(专门用来写sql语句的)
mybatis:
  mapper-locations: classpath:mapper/*.xml
